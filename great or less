#include<stdio.h>

int main()

{
    int x,y,z;
    scanf("%d%d%d",&x,&y,&z);
    if((x>y) && (x>z)){
        printf("x is greater");
    }
    else if ((y>x)&&("y is greater");
    else printf("both are equal");
    return 0;
}
